.pop__mask {
position:fixed;
top:0;
right:0;
bottom:0;
left:0;
z-index:10;
display:block;
background:rgba(0,0,0,.7);
}

.pop {
  display:none;
	visibility: hidden;
}

.pop--show {
	visibility: visible;
}